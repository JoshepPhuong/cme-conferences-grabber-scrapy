# ? AWS secret: https://us-east-1.console.aws.amazon.com/secretsmanager/secret?name=usummit-prod-spiderkeeper&region=us-east-1
# ? Generated by gomplate via tekton pipeline grabbers-build-pipeline at {{ (time.Now).Format time.UnixDate }}
# ---
# Env file to simplify local run of spiders using IDEs
# For PyCharm set up plugin https://github.com/Ashald/EnvFile
PYTHONPATH=.:$PYTHONPATH
SCRAPY_SETTINGS_MODULE={{ .Env.grabbers_settings }}
